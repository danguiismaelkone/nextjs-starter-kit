node_modules
.next
.git
.github
.vscode

# tests — playwright.config.ts/vitest.config.ts excluded too: `next build`'s
# typecheck scans every *.ts at the repo root (tsconfig.json), and
# playwright.config.ts imports from ./e2e/global-setup, which would otherwise
# be a dangling reference once e2e/ itself is excluded below.
coverage
test-results
playwright-report
e2e
playwright.config.ts
vitest.config.ts
lib/**/*.test.ts
app/**/*.test.ts

# env (jamais dans l'image — injecté au runtime par docker-compose/la plateforme de déploiement)
.env
.env.*

*.log
npm-debug.log*
pnpm-debug.log*

.DS_Store
README.md
backlog
